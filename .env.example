# LLM provider keys — only set the one you intend to use
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional — improves Semantic Scholar rate limits if you have one
# https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=
